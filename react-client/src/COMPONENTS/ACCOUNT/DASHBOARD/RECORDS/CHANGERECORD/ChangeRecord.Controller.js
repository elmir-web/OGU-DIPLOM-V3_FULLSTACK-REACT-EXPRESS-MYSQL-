import Toast from './../../../../../Toast';

import ChangeRecordService from './ChangeRecord.Service';

const ChangeRecordController = () => {
  setTimeout(() => {
    setTimeout(async () => {}, 1000);
  }, 1000);
};

export default ChangeRecordController;
