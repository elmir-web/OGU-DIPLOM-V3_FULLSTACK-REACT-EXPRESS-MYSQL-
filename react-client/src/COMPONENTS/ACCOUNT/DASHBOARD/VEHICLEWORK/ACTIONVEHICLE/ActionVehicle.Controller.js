import Toast from '../../../../../Toast';

import ActionVehicleService from './ActionVehicle.Service';

const ActionVehicleController = async () => {
  setTimeout(() => {
    setTimeout(async () => {}, 1000);
  }, 1000);
};

export default ActionVehicleController;
