import Toast from '../../../../../Toast';

import RemoveItemFillingListService from './RemoveItemFillingList.Service';

const RemoveItemFillingListController = () => {
  setTimeout(() => {
    setTimeout(async () => {}, 1000);
  }, 1000);
};

export default RemoveItemFillingListController;
