import Toast from './../../../../../Toast';

import ChangeItemFillingListService from './ChangeItemFillingList.Service';

const ChangeItemFillingListController = () => {
  setTimeout(() => {
    setTimeout(async () => {}, 1000);
  }, 1000);
};

export default ChangeItemFillingListController;
